# Browser talks same-origin /api/admin (no mixed-content).
NEXT_PUBLIC_API_BASE=/api/admin

# Server-side rewrite target — VPS agent (HTTP ok: only Vercel server calls it).
API_URL=http://178.88.115.213:3044
